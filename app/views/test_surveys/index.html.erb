<% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_user_session_path, method: :delete) %>        
  </li>
<% end %> <br/>

<% if !@surveys.blank? %>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>


  <tbody>
    <% @surveys.each do |survey| %>
      <tr>
      	<td> <%= survey.name %> </td>
        <td><%= link_to 'Show', survey %></td>
        <% if current_user.is_admin == true %>
	        <td><%= link_to 'Edit', edit_test_survey_path(survey) %></td>
	        <td><%= link_to 'Destroy', survey, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
<% end %>

<br/>

<%= link_to 'New Survey', new_test_survey_path %>